import { ModulePage } from "@/modules/shared/ModulePage";

export default function LocalizedCOA() {
  return <ModulePage title="Localized COA" subtitle="Map local chart of accounts to standard structure" features={["Account Mapping", "Country Templates", "Consolidation Rules", "Validation"]} />;
}
